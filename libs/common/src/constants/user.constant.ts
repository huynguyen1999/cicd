export enum UserStatus {
  Online = 'online',
  Offline = 'offline',
  Banned = 'banned',
  Deleted = 'deleted',
}

export enum UserRole {
  User = 'user',
  PremiumUser = 'premium_user',
  Admin = 'admin',
  Moderator = 'moderator',
}
